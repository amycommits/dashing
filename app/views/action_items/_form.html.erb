<%= form_for(@action_item) do |f| %>
  <% if @action_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@action_item.errors.count, "error") %> prohibited this action_item from being saved:</h2>

      <ul>
      <% @action_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="field">
  <%= f.label :accession_number %><br>
  <% options = options_from_collection_for_select(@projects, 'accession_number', 'accession_number') %>
<%= f.select :accession_number,  options %>
</div>

  <div class="field">
    <%= f.label :project_id %><br>
    <% options = options_from_collection_for_select(@projects,'id','title') %>
    <%= f.select :project_id,  options %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
